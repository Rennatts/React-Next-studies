# Next.js — server

Notes in this folder focus on **server-side behavior in the Next.js App Router**: where code runs by default, how data and mutations flow without shipping logic to the client, and how that differs from classic client-only React.

## Topics

| Topic | Folder | Route |
|-------|--------|--------|
| Server-side in the App Router | [`server-side`](./server-side/README.md) (notes + [`ServerSideRscFetchDemo.tsx`](./server-side/ServerSideRscFetchDemo.tsx)) | `/studies/next-server/server-side` |

Add more rows here as you split out caching, auth, Route Handlers, etc.
